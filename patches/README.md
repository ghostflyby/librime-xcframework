# Upstream Patches

Put upstream source patches here as `*.patch` files. `scripts/apply-patches.sh` applies them in lexical order after copying or archiving the selected upstream source ref into `.build/src-<arch>/`.

Keep patches limited to build and packaging compatibility changes. Do not put librime feature work here.

